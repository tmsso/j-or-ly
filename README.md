# python-playground

## J vagy LY web app

Run locally:

```bash
streamlit run app/jly_game.py
```

The app uses `data/magyar-szavak.txt` to generate correct/wrong spelling pairs focused on Hungarian `j` and `ly` confusion.
